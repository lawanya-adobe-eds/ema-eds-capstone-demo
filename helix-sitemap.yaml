# Diagnostic: fresh destination filename (/sitemap-v2.xml) to bypass the stuck
# /sitemap.xml cache and test whether `origin` resolves the <loc> host.
sitemaps:
  default:
    origin: https://main--ema-eds-capstone-demo--lawanya-adobe-eds.aem.live
    languages:
      pages:
        source: /us/en/pages-index.json
        destination: /sitemap-v2.xml
        hreflang: en-US
      adventures:
        source: /us/en/adventures/query-index.json
        destination: /sitemap-v2.xml
        hreflang: en-US
      magazine:
        source: /us/en/magazine/query-index.json
        destination: /sitemap-v2.xml
        hreflang: en-US
      faqs:
        source: /us/en/faqs/faq-index.json
        destination: /sitemap-v2.xml
        hreflang: en-US
      about:
        source: /us/en/about-us/people-index.json
        destination: /sitemap-v2.xml
        hreflang: en-US
